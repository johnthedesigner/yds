export const CompactTextWrapper = (props) => {
  return <div className="compact-text__wrapper">{props.children}</div>;
};

export const CompactText = (props) => {
  return <div className="compact-text__item">{props.children}</div>;
};

export default CompactText;
